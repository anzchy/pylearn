# 部署指南

本文档详细介绍了如何将 PyLearn 网站部署到不同的平台。

## 📦 部署前准备

### 1. 完成本地测试
确保网站在本地正常运行：
```bash
# 安装依赖
pip install mkdocs-material

# 本地测试
mkdocs serve
```

### 2. 推送代码到 Git
```bash
git add .
git commit -m "Ready for deployment"
git push origin master
```

## 🚀 部署选项

### 选项一：GitHub Pages（推荐）⭐

**优点**：
- ✅ 完全免费
- ✅ 与 GitHub 无缝集成
- ✅ 自动部署（使用 GitHub Actions）
- ✅ 自带 HTTPS
- ✅ 100GB/月 免费流量

#### 方法 A：GitHub Actions 自动部署（推荐）

本项目已配置 GitHub Actions，**无需手动运行任何命令**，只需推送代码即可自动部署！

**步骤 1：推送代码到 GitHub**

```bash
git add .
git commit -m "Update content"
git push origin master
```

**步骤 2：配置 GitHub Pages**

1. 访问仓库设置页面：
   ```
   https://github.com/你的用户名/pylearn/settings/pages
   ```

2. 在 **"Build and deployment"** 部分配置：
   - **Source**: 选择 `Deploy from a branch`
   - **Branch**:
     - 第一个下拉菜单选择 `gh-pages`
     - 第二个下拉菜单选择 `/ (root)`
   - 点击 **Save** 按钮

3. 等待部署完成（1-3 分钟）

4. 访问网站：
   ```
   https://你的用户名.github.io/pylearn/
   ```

**步骤 3：查看部署状态**

访问 Actions 页面查看构建进度：
```
https://github.com/你的用户名/pylearn/actions
```

看到绿色的 ✅ 表示部署成功！

**工作原理：**

项目中的 `.github/workflows/deploy.yml` 文件会自动：
1. 监听 master 分支的推送
2. 安装 Python 3.9 和依赖
3. 运行 `mkdocs build` 构建网站
4. 推送到 `gh-pages` 分支
5. GitHub Pages 自动发布

---

#### 方法 B：手动部署（备选）

如果 GitHub Actions 不可用，可以手动部署：

```bash
# 激活虚拟环境
source .venv/bin/activate

# 一键部署到 GitHub Pages
mkdocs gh-deploy --clean
```

此命令会自动：
- 构建网站到 `site/` 目录
- 创建/更新 `gh-pages` 分支
- 推送到 GitHub

---

#### 方法 C：手动推送 gh-pages 分支

如果网络不稳定，可以手动操作：

```bash
# 1. 构建网站
source .venv/bin/activate
mkdocs build

# 2. 进入 site 目录
cd site

# 3. 初始化 git 并推送
git init
git add -A
git commit -m "Deploy to GitHub Pages"
git branch -M gh-pages
git remote add origin https://github.com/你的用户名/pylearn.git
git push -f origin gh-pages

# 4. 返回项目根目录
cd ..
```

---

#### 🎯 GitHub Pages 配置详解

**重要说明**：

1. **为什么首页显示 README.md？**
   - 如果 GitHub Pages 设置为从 `master` 分支的根目录部署，会显示 README.md
   - 正确做法是使用 `gh-pages` 分支部署 MkDocs 构建的网站

2. **分支选择：**
   - ❌ **错误**: `master` 分支 + `/ (root)` → 显示 README.md
   - ✅ **正确**: `gh-pages` 分支 + `/ (root)` → 显示 MkDocs 网站

3. **配置位置：**
   ```
   GitHub 仓库 → Settings → Pages → Build and deployment
   ```

**配置示意图：**

```
┌─────────────────────────────────────────────┐
│ Build and deployment                        │
├─────────────────────────────────────────────┤
│ Source                                      │
│ ┌─────────────────────────────────────────┐ │
│ │ ▼ Deploy from a branch                  │ │
│ └─────────────────────────────────────────┘ │
├─────────────────────────────────────────────┤
│ Branch                                      │
│ ┌─────────────┐  ┌─────────┐  ┌──────────┐ │
│ │ ▼ gh-pages  │  │ ▼ /(root)│  │   Save   │ │
│ └─────────────┘  └─────────┘  └──────────┘ │
└─────────────────────────────────────────────┘
```

---

#### 📋 常见问题 (GitHub Pages)

**Q1: 首页显示的是 README 而不是网站？**

**A:** 检查 GitHub Pages 配置：
- 访问 Settings → Pages
- 确保 Branch 选择的是 `gh-pages`，不是 `master`
- 如果没有 `gh-pages` 分支，运行 `mkdocs gh-deploy` 或等待 GitHub Actions 完成

**Q2: 404 错误 - 页面不存在？**

**A:** 可能原因：
1. 部署未完成 - 等待 1-3 分钟
2. 分支选择错误 - 检查是否选择了 `gh-pages`
3. 网站未构建 - 检查 Actions 是否成功

**Q3: 如何手动触发重新部署？**

**A:** 两种方法：
1. 推送任意更改到 master 分支
2. 访问 Actions 页面 → 选择 workflow → 点击 "Run workflow"

**Q4: Actions 构建失败怎么办？**

**A:** 检查以下几点：
1. 查看 Actions 页面的错误日志
2. 确保 `requirements.txt` 存在且正确
3. 确保 `mkdocs.yml` 配置正确
4. 检查仓库 Actions 权限是否启用：
   ```
   Settings → Actions → General → Workflow permissions
   选择 "Read and write permissions"
   ```

**Q5: 能否使用自定义域名？**

**A:** 可以！步骤：
1. 在 DNS 提供商处添加 CNAME 记录：
   ```
   CNAME    www    你的用户名.github.io
   ```
2. 在 GitHub Pages 设置中输入自定义域名
3. 等待 DNS 生效（可能需要几小时）

---

#### 🔄 自动部署工作流

每次推送到 master 分支时：

```
推送代码 (git push)
    ↓
GitHub Actions 触发
    ↓
安装 Python 3.9
    ↓
安装依赖 (pip install -r requirements.txt)
    ↓
构建网站 (mkdocs build)
    ↓
推送到 gh-pages 分支
    ↓
GitHub Pages 自动发布
    ↓
网站更新完成 ✅
```

**时间线：**
- 代码推送：0 秒
- Actions 启动：10-30 秒
- 构建完成：1-2 分钟
- Pages 发布：30 秒-1 分钟
- **总计：约 2-4 分钟**

---

### 选项二：腾讯云 EdgeOne Pages

**优点**：
- 国内访问速度快
- 支持 CDN 加速
- 免费额度充足

#### 详细部署步骤：

1. **登录腾讯云控制台**
   - 访问：https://console.cloud.tencent.com/
   - 搜索 "EdgeOne"

2. **创建 Pages 应用**
   - 选择 "EdgeOne" → "Pages"
   - 点击 "新建应用"

3. **配置 Git 仓库**
   ```
   仓库类型：GitHub
   仓库地址：https://github.com/anzchy/pylearn.git
   分支：master
   构建目录：site
   ```

4. **构建设置** ⚠️ **关键配置**

   **方法一：使用 pip3 和 requirements.txt（推荐）**
   ```bash
   # 构建命令
   pip3 install --upgrade pip && pip3 install -r requirements.txt && mkdocs build

   # 输出目录
   site
   ```

   **方法二：使用构建脚本**
   ```bash
   # 构建命令
   bash build.sh

   # 输出目录
   site
   ```

5. **环境变量（必需）**

   在 EdgeOne Pages 控制台的"环境变量"设置中添加：
   ```
   PYTHON_VERSION=3.11
   ```

   **说明**：默认使用 Python 2.7 会导致构建失败，必须指定 Python 3.x

6. **域名配置**
   - 默认域名：`xxx.pages.edgeone.app`
   - 自定义域名：需要备案

### 选项三：Vercel

**优点**：
- 全球 CDN
- 自动 HTTPS
- 预览部署

**步骤**：
1. 访问 [vercel.com](https://vercel.com)
2. 导入 GitHub 仓库
3. 配置构建设置：
   ```json
   {
     "buildCommand": "pip install mkdocs-material && mkdocs build",
     "outputDirectory": "site",
     "installCommand": "pip install mkdocs-material"
   }
   ```

### 选项四：Netlify

**优点**：
- 表单处理
- 分支预览
- API 访问

**步骤**：
1. 访问 [netlify.com](https://netlify.com)
2. 连接 GitHub 仓库
3. 构建设置：
   ```
   Build command: pip install mkdocs-material && mkdocs build
   Publish directory: site
   ```

## 🔧 常见部署问题

### 构建失败

**问题**：`mkdocs-material` 安装失败
```bash
# 解决方案
pip install --upgrade pip
pip install mkdocs-material
```

**问题**：PyScript 加载失败
```bash
# 检查网络连接
# 确保使用最新版本的 PyScript
```

### 域名问题

**问题**：自定义域名无法访问
1. 检查 DNS 配置
2. 确保域名已备案（国内服务器）
3. 检查 SSL 证书状态

### 性能优化

**建议**：
1. 启用 Gzip 压缩
2. 配置缓存策略
3. 使用 CDN 加速
4. 优化图片大小

## 📊 部署平台对比

| 平台 | 免费额度 | 国内访问 | 自定义域名 | 构建时间 | 自动部署 | 推荐度 |
|------|---------|---------|-----------|---------|---------|--------|
| **GitHub Pages** | 100GB/月 | 慢 | ✅ 支持 | 2-4分钟 | ✅ GitHub Actions | ⭐⭐⭐⭐⭐ |
| **EdgeOne Pages** | 10GB/月 | 快 | ✅ 支持（需备案） | 1-5分钟 | ✅ Git Push | ⭐⭐⭐⭐ |
| **Vercel** | 100GB/月 | 中等 | ✅ 支持 | 1-3分钟 | ✅ Git Push | ⭐⭐⭐⭐ |
| **Netlify** | 100GB/月 | 中等 | ✅ 支持 | 1-5分钟 | ✅ Git Push | ⭐⭐⭐ |

**平台选择建议：**

- **🌍 海外用户** → GitHub Pages（最佳）
- **🇨🇳 国内用户** → EdgeOne Pages（国内访问快）
- **🚀 需要预览** → Vercel（支持 PR 预览）
- **📊 需要表单** → Netlify（内置表单处理）

## 🔄 自动部署

所有推荐平台都支持自动部署：

1. **触发条件**：代码推送到指定分支
2. **构建流程**：
   ```
   拉取代码 → 安装依赖 → 构建网站 → 部署上线
   ```
3. **部署时间**：通常 1-10 分钟

## 📈 监控与分析

### 推荐工具
- **Google Analytics**：访问统计
- **百度统计**：国内用户分析
- **Cloudflare Analytics**：性能监控

### 重要指标
- 页面加载时间
- 用户访问量
- 错误率
- CDN 命中率

## 🛠️ 维护建议

### 定期更新
```bash
# 更新依赖
pip install --upgrade mkdocs-material

# 检查 PyScript 更新
# 访问 https://pyscript.net/
```

### 备份策略
- Git 代码备份
- 定期导出网站内容
- 备份自定义配置

### 安全检查
- 定期检查依赖漏洞
- 监控异常访问
- 更新 SSL 证书