# 部署指南

本文档详细介绍了如何将 PyLearn 网站部署到不同的平台。

## 📦 部署前准备

### 1. 完成本地测试
确保网站在本地正常运行：
```bash
# 安装依赖
pip install mkdocs-material

# 本地测试
mkdocs serve
```

### 2. 推送代码到 Git
```bash
git add .
git commit -m "Ready for deployment"
git push origin master
```

## 🚀 部署选项

### 选项一：GitHub Pages（推荐新手）

**优点**：
- 免费
- 与 GitHub 无缝集成
- 自动部署

**步骤**：
```bash
# 一键部署到 GitHub Pages
mkdocs gh-deploy
```

访问：`https://anzchy.github.io/pylearn`

### 选项二：腾讯云 EdgeOne Pages

**优点**：
- 国内访问速度快
- 支持 CDN 加速
- 免费额度充足

#### 详细部署步骤：

1. **登录腾讯云控制台**
   - 访问：https://console.cloud.tencent.com/
   - 搜索 "EdgeOne"

2. **创建 Pages 应用**
   - 选择 "EdgeOne" → "Pages"
   - 点击 "新建应用"

3. **配置 Git 仓库**
   ```
   仓库类型：GitHub
   仓库地址：https://github.com/anzchy/pylearn.git
   分支：master
   构建目录：site
   ```

4. **构建设置**
   ```bash
   # 构建命令
   pip install mkdocs-material && mkdocs build

   # 输出目录
   site
   ```

5. **环境变量（可选）**
   ```
   PYTHON_VERSION: 3.9
   ```

6. **域名配置**
   - 默认域名：`xxx.pages.edgeone.app`
   - 自定义域名：需要备案

### 选项三：Vercel

**优点**：
- 全球 CDN
- 自动 HTTPS
- 预览部署

**步骤**：
1. 访问 [vercel.com](https://vercel.com)
2. 导入 GitHub 仓库
3. 配置构建设置：
   ```json
   {
     "buildCommand": "pip install mkdocs-material && mkdocs build",
     "outputDirectory": "site",
     "installCommand": "pip install mkdocs-material"
   }
   ```

### 选项四：Netlify

**优点**：
- 表单处理
- 分支预览
- API 访问

**步骤**：
1. 访问 [netlify.com](https://netlify.com)
2. 连接 GitHub 仓库
3. 构建设置：
   ```
   Build command: pip install mkdocs-material && mkdocs build
   Publish directory: site
   ```

## 🔧 常见部署问题

### 构建失败

**问题**：`mkdocs-material` 安装失败
```bash
# 解决方案
pip install --upgrade pip
pip install mkdocs-material
```

**问题**：PyScript 加载失败
```bash
# 检查网络连接
# 确保使用最新版本的 PyScript
```

### 域名问题

**问题**：自定义域名无法访问
1. 检查 DNS 配置
2. 确保域名已备案（国内服务器）
3. 检查 SSL 证书状态

### 性能优化

**建议**：
1. 启用 Gzip 压缩
2. 配置缓存策略
3. 使用 CDN 加速
4. 优化图片大小

## 📊 部署平台对比

| 平台 | 免费额度 | 国内访问 | 自定义域名 | 构建时间 |
|------|---------|---------|-----------|---------|
| GitHub Pages | 100GB/月 | 慢 | 支持 | 2-10分钟 |
| EdgeOne Pages | 10GB/月 | 快 | 支持 | 1-5分钟 |
| Vercel | 100GB/月 | 中等 | 支持 | 1-3分钟 |
| Netlify | 100GB/月 | 中等 | 支持 | 1-5分钟 |

## 🔄 自动部署

所有推荐平台都支持自动部署：

1. **触发条件**：代码推送到指定分支
2. **构建流程**：
   ```
   拉取代码 → 安装依赖 → 构建网站 → 部署上线
   ```
3. **部署时间**：通常 1-10 分钟

## 📈 监控与分析

### 推荐工具
- **Google Analytics**：访问统计
- **百度统计**：国内用户分析
- **Cloudflare Analytics**：性能监控

### 重要指标
- 页面加载时间
- 用户访问量
- 错误率
- CDN 命中率

## 🛠️ 维护建议

### 定期更新
```bash
# 更新依赖
pip install --upgrade mkdocs-material

# 检查 PyScript 更新
# 访问 https://pyscript.net/
```

### 备份策略
- Git 代码备份
- 定期导出网站内容
- 备份自定义配置

### 安全检查
- 定期检查依赖漏洞
- 监控异常访问
- 更新 SSL 证书