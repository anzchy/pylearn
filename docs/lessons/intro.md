# Python 基础语法

Python 是一种强大的解释型语言。以下示例展示了函数的定义与调用。

**提示**：可以编辑代码后点击右上角的 **Run** 按钮运行，或使用 **Ctrl+Enter** 快捷键。

<script type="py-editor">
def greet(name):
    return f"你好, {name}!"

result = greet("学生")
print(result)
</script>
